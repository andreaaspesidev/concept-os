[package]
name = "coverage_tests"
version = "0.1.0"
edition = "2021"

[features]
default = ["swap"]
swap = []

[dependencies]
abi = {path = "../../../../sys/abi"}
buddy_allocator = {path = "../../../buddy_allocator", default_features=false}
heapless = "0.7.13" # Stack data structures
serde = { version = "1.0.114", default-features = false }