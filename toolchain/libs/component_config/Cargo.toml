[package]
name = "component_config"
version = "0.0.2"
edition = "2021"

[dependencies]
toml = "0.5.9"
serde = {version = "1.0.137", features=["derive"]}
serde-hex = "0.1.0"

[dev-dependencies]
tempfile = "3.3.0"